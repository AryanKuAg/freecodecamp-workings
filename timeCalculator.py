def add_time(time, duration, day="0"):
    isAm = True if "AM" in time else False
    timeHour = time.split(":")[0]
    timeMinute = time.split(":")[:2]
    durationHour = duration.split(":")[0]
    durationMinute = duration.split(":")[:2]
    # time

    # duration

    # day

    # add_time("3:00 PM", "3:10")
    # # Returns: 6:10 PM
